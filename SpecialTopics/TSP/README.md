My Programming Environment is macOS Mojave 10.14.4

To compile the program, one may use the following command:

gcc -lstdc++ -o main main.cpp


Before running the program, make sure you have input.txt in the same directory as an input. Then you can run the following command to test the program.

./main